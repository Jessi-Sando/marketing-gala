# Copiá este archivo como ".env" (sin ".example") y completá los valores reales.
# El archivo ".env" nunca se sube a git (está en .gitignore).

FB_APP_ID=tu_app_id_de_meta
FB_APP_SECRET=tu_clave_secreta_de_meta
FB_SHORT_LIVED_TOKEN=el_token_que_copiaste_del_explorador_de_la_api_graph
